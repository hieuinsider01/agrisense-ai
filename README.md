# agrisense-ai

The beginning of a new future for agriculture

## Structure of project:

agrisense-ai/
│
├── index.html
│
├── assets/
│ ├── images/
│ ├── fonts/
│ └── icons/
│
├── styles/
│ ├── base.css ← reset, variables
│ ├── layout.css ← bố cục to như header, footer, grid
│ └── components.css ← nút bấm, card, modal...
│
├── scripts/
│ ├── main.js ← logic chính
│ └── utils.js ← các hàm nhỏ hỗ trợ
│
└── data/
└── mock.json ← dữ liệu demo tạm cho UI
